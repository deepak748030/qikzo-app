{
  "name": "qikzo-workspace",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "node scripts/placeholder-server.js",
    "build": "mkdir -p dist && [ -f dist/index.html ] || echo '<!doctype html><title>Qikzo</title><h1>Qikzo Workspace</h1>' > dist/index.html",
    "build:dev": "mkdir -p dist && [ -f dist/index.html ] || echo '<!doctype html><title>Qikzo</title><h1>Qikzo Workspace</h1>' > dist/index.html"
  }
}
